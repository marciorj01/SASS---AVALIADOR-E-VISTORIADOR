Options -MultiViews
RewriteEngine On

# Mantém arquivos e diretórios reais, incluindo assets, manifest e service worker.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Fallback para a aplicação React.
RewriteRule ^ index.html [L]
